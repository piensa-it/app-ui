# Backlog del producto — Mis Finanzas

Última actualización: 2026-07-21 · Este archivo es la fuente de verdad del backlog.
Convención de estado: ✅ Hecho · 🔨 En curso · 📋 Pendiente · 🤔 Requiere decisión del propietario.

Regla de trabajo: **cada funcionalidad liberada debe entregar su manual de usuario**
en `src/content/manuals/` (visible en el menú Ayuda de la app) y actualizar este backlog.

## ⚠️ Riesgo activo: Lovable sigue sincronizando a `main`

2026-07-19: se detectaron commits (`f72c979`…`dcf45d1`) empujados por el editor de
Lovable —que sigue conectado al mismo repo de GitHub— con mejoras a `Landing.tsx`.
De paso sobrescribieron `src/integrations/supabase/types.ts` con el esquema del
proyecto VIEJO (`bymdreiwahvjejhjubrp`), borrando silenciosamente `audit_logs`,
`budgets` y `recurring_transactions` de los tipos. Ya corregido (types regenerados
desde el proyecto nuevo, commit `edc04f9`), pero **puede volver a pasar** mientras
Lovable siga con push access al repo. Ver P4-16: desconectar/cerrar Lovable es la
única forma de eliminar este riesgo de raíz.

## P0 — Bloqueantes para usar lo ya construido

| # | Ítem | Estado | Notas |
|---|---|---|---|
| P0-1 | Aplicar migraciones al proyecto Supabase nuevo ("misfin", `jlovwiskabbnbxkhstdy`) | ✅ | 48 migraciones aplicadas vía `db push`; datos completos del backup de Lovable restaurados (15 usuarios con contraseñas originales, 175 categorías, 461 subcategorías, 46 cuentas, 135 transacciones, etc. — conteos verificados 1:1) |
| P0-2 | Regenerar `types.ts` tras migrar | ✅ | Regenerado 2026-07-19 (commit `edc04f9`) tras detectar que un commit de Lovable lo había revertido al esquema viejo |
| P0-3 | Configurar `EMAIL_WEBHOOK_SECRET`, `ANTHROPIC_API_KEY`, `RESEND_API_KEY` y `CRON_SECRET` en Supabase | 🤔 | Pendiente de confirmación del propietario — instrucciones enviadas (`supabase secrets set`); `CRON_SECRET` es nuevo (2026-07-19, para `process-email-queue`) y ya tiene valor generado y guardado en Supabase Vault del lado de la BD, falta configurarlo como secret de función |
| P0-4 | Desplegar edge function `process-email-transaction` v2 | ✅ | 9 funciones desplegadas y verificadas ACTIVE en el proyecto nuevo |
| P0-5 | Corregir grants `PUBLIC`/`anon` en funciones internas del proyecto nuevo | ✅ | `get_advisors` detectó que el REVOKE de `20260719000100` no tuvo efecto (el privilegio estaba en `PUBLIC`, no en `anon`/`authenticated`). Corregido en `20260719160000_fix_public_grants.sql`; verificado función por función |
| P0-6 | Cola asíncrona para ingesta de correos (pgmq + pg_cron) | ✅ | El webhook ya no procesa síncrono; encola y un consumidor (`process-email-queue`) disparado por `pg_cron` cada minuto hace la extracción en lotes. Evita picos a la BD y a la API de Anthropic. Ver `docs/email-ingestion-design.md` |
| P0-7 | Incidente: sitio caído en producción tras deploy de GTM/Consent | ✅ | 2026-07-20: `CookieConsent` (usa `<Link>` de react-router) se montaba fuera de `<BrowserRouter>` en `App.tsx` — lanzaba en cada render, capturado por `ErrorBoundary`, mostrando "Algo salió mal" para todo visitante sin consentimiento decidido. Corregido en commit `91e84b2`. Ningún test cubría el árbol de montaje de `App.tsx` — gap real, ver P4-9 |

## P1 — Núcleo financiero (Fase 1)

| # | Ítem | Estado | Notas |
|---|---|---|---|
| P1-1 | Modelo activo/pasivo con tarjetas de crédito | ✅ | Compra = gasto+deuda; pago = transferencia |
| P1-2 | Transferencias atómicas entre cuentas (RPC) | ✅ | Corrigió doble conteo |
| P1-3 | Presupuestos con umbral de alerta | ✅ | Página /budgets |
| P1-4 | Recurrentes persistidos e idempotentes | ✅ | Índice único en BD |
| P1-5 | Cálculos centralizados (`services/cashflow.ts`) | ✅ | 57 tests |
| P1-6 | Campos de cuenta: entidad, saldo inicial, corte/pago en formulario de PaymentMethods | ✅ | Incluye últimos 4 dígitos |
| P1-7 | Unificar `Reports.tsx` con la tabla `budgets` (hoy usa `categories.presupuesto`) | ✅ | `budgets` es fuente principal; presupuesto legado de categorías queda como respaldo |
| P1-8 | Migrar traslados históricos (gasto+ingreso "Traslado"/"Pago tarjeta") a tipo `transferencia` | 🤔 | Script opcional; afecta cifras históricas |
| P1-9 | Paginación real por rangos + agregados en servidor | ✅ | 2026-07-21: la página de Transacciones ya no carga todo el historial (tope 5.000) para filtrar/sumar en el navegador. Nuevo hook `useTransactionsQuery.ts`: `useTransactionsPage` (consulta con `.range()` + `count:'exact'`, filtros al servidor: tipo/categoría/medio de pago/fechas/búsqueda), `useTransactionsSummary` (agregados vía RPC nueva `get_transactions_summary`, migración `20260721150000` — suma por moneda/tipo; la conversión a la moneda del usuario sigue en el cliente) y `fetchAllFilteredTransactions` (export CSV trae todo el conjunto filtrado en lotes, bajo demanda). `useTransactions({list:false})` evita disparar la consulta grande en esta página (solo usa las mutaciones). **De paso se corrigió un bug real**: el filtro de "medio de pago" comparaba contra las etiquetas fijas "Efectivo/Banco/Tarjeta" pero `medio_pago` guarda el nombre real de la cuenta — ahora los checkboxes se arman con las cuentas activas reales. Índice nuevo `idx_transactions_user_fecha`. `types.ts` parchado a mano con la firma del RPC (regenerar tras aplicar la migración). Verificado con `tsc --noEmit` y `eslint` (sin errores nuevos; los 2 `any` que reporta son preexistentes). **Falta**: que el propietario aplique la migración (`supabase db push`) y regenere types. Nota: el dashboard (`Index.tsx`) todavía calcula agregados sobre todas las filas en el navegador — pendiente de migrar en un paso aparte (ver P1-9b) |
| P1-9b | Migrar agregados de Reportes y dashboard a servidor | ✅ | 2026-07-21: **Dashboard hecho** — `Index.tsx` ya no llama `useTransactions()` (no carga el historial completo). Al investigar se encontró la causa raíz: el diálogo nunca guardaba `payment_method_id` (las 135 transacciones lo tenían NULL), así que la vista `account_balances` no contaba movimientos. Se arregló primero eso (ver commit del backfill + fix del diálogo). Con la vista ya correcta, el dashboard ahora usa: `useAccountBalances` (saldos por cuenta + patrimonio neto, unido a `usePaymentMethods` por icono/color), `get_transactions_summary` (totales ingresos/gastos por moneda→convertidos en cliente), `get_category_spending` (gráfica de torta = histórico; comparación vs. presupuesto = mes actual/anterior), nueva RPC `get_monthly_category_spending` (migración `20260721190000`) + hook (serie de la gráfica de evolución), y `useRecentTransactions` (últimas 10 por rango). Las 3 gráficas conservan su contrato (`transactions`): se les pasan "pseudo-transacciones" derivadas de los agregados (una por categoría/mes con su total), que al agrupar dan el mismo resultado sin traer todas las filas. Verificado con `tsc` y `eslint` (sin errores nuevos; los `any` son casts preexistentes). **Requiere del propietario**: aplicar migraciones (`20260721170000`, `20260721180000` backfill, `20260721190000`) con `db push`, regenerar types, y **verificar visualmente el dashboard con la app corriendo** (no fue posible en el sandbox) — en especial que los saldos por cuenta y los totales cuadren con lo esperado. Nota menor: la vista suma `valor` en crudo, mientras el cálculo viejo convertía cada movimiento a la moneda del usuario; con datos casi 100% COP (solo 2 movimientos USD) la diferencia es marginal, y la vista ya era la fuente de verdad del resto de la app. **Reportes** (fila P1-9, arriba) ya estaba migrado. |
| ~~P1-9b-old~~ | (histórico) Reportes migrado | ✅ | 2026-07-21: **Reportes** — nueva RPC `get_category_spending(p_date_from, p_date_to)` (migración `20260721170000`, suma `gasto` por `category_id` del rango, réplica exacta de la semántica de `budgetExecution`) + hook `useCategorySpending.ts`. `Reports.tsx` ya no llama `useTransactions()` (no carga el historial completo): el gasto por categoría del mes viene agregado del servidor; el presupuesto "global" usa la suma de todas las filas de la RPC (incluidos gastos sin categoría). `types.ts` parchado a mano con la firma. Verificado con `tsc`/`eslint` (los 2 `any` que reporta son preexistentes). **Falta el dashboard** (`Index.tsx`): sus 3 gráficas (CategoryChart, MonthlyGrowthChart, BudgetComparisonChart) + totales del mes + saldos por cuenta siguen sobre todas las filas. Es la parte más grande y de mayor riesgo (cifras de la pantalla principal) y necesita verificación visual con la app corriendo (no posible en sandbox) — se deja como paso dedicado. Pista: los saldos por cuenta ya existen server-side en la vista `account_balances`/`useAccountBalances`, y los totales del mes pueden reusar `get_transactions_summary` (P1-9) con el rango del mes; faltarían RPCs para el desglose por categoría (ya existe `get_category_spending`, reutilizable) y la serie mensual de crecimiento |

## P2 — Ingesta de correos (Fase 2)

| # | Ítem | Estado | Notas |
|---|---|---|---|
| P2-1 | Pipeline de sugerencias con confianza y dedup idempotente | ✅ | Nunca auto-confirma |
| P2-2 | UI confirmar/corregir/descartar | ✅ | /email-staging |
| P2-3 | Parser Bancolombia + genérico + contrato extensible | ✅ | |
| P2-4 | Parsers adicionales (Nequi, Davivienda, BBVA, Daviplata…) | 🔨 | Nequi, Davivienda y DaviPlata ✅; BBVA y otros pendientes |
| P2-5 | Match automático de tarjeta por últimos 4 dígitos | ✅ | Migración `20260718000500`; preselección en confirmar/corregir |
| P2-6 | Conexión OAuth de buzón por usuario (Gmail/Outlook) | 🤔 | Decisión de proveedor |
| P2-7 | Alerta cuando un correo no se pudo leer (ni parser ni IA lo identificaron) | 🤔 | 2026-07-21: pedido del propietario para ir ampliando parsers/keywords de forma dirigida en vez de adivinando. Diseño en `docs/email-ingestion-design.md`. Pendiente decidir canal (¿correo al propietario vía Resend, que ya se usa en el proyecto? ¿digest diario en vez de uno por correo?) y cómo distinguir "correo bancario que no supimos leer" de "correo que simplemente no es financiero" (hoy ambos caen en `status=ignored`, ver detalle en el diseño) |
| P2-8 | Leer extractos/resúmenes de cuenta (no solo movimientos individuales) | 🤔 | 2026-07-21: pedido del propietario — los extractos también son "información de interés para concentrar los datos financieros". Es un tipo de dato distinto (agregado: saldo, cupo, fecha de corte/pago) al que maneja hoy el pipeline (`ExtractedTransaction`, movimiento puntual). Necesita su propio contrato de extracción y probablemente tabla propia (`account_statements`) en vez de forzarlo en `email_staging`/`transactions`. Ver `docs/email-ingestion-design.md` |

## P3 — Tarjetas avanzado y flujo proyectado (Fase 3)

| # | Ítem | Estado | Notas |
|---|---|---|---|
| P3-1 | Compras a cuotas/diferidos: plan de cuotas + proyección de flujo de efectivo | 📋 | Decisión tomada: gasto completo al comprar; cuotas solo afectan flujo proyectado |
| P3-2 | Pago estimado de tarjeta según corte/fecha de pago | 📋 | "Próximos compromisos" en dashboard |
| P3-3 | Evolución del patrimonio (serie histórica) | 📋 | |

## P4 — Plataforma y calidad

| # | Ítem | Estado | Notas |
|---|---|---|---|
| P4-1 | Backlog versionado + manuales de usuario en menú Ayuda | ✅ | Página /ayuda con 7 manuales; registro en `src/lib/manuals.ts` |
| P4-2 | Manuales por cada funcionalidad nueva liberada | 🔨 | Regla permanente. Barrido completo hecho: 12 manuales cubren todas las pantallas funcionales (los simuladores se documentarán cuando se decida su futuro, P4-10) |
| P4-3 | Tabla `merchants` + persistir Establecimientos | ✅ | 2026-07-21: la página de Establecimientos ya no es fachada (antes: datos de ejemplo hardcodeados + `useState`, se perdía todo al recargar). Nueva tabla `merchants` con RLS (migración `20260721160000`: user_id, nombre, etiquetas text[], contacto/telefono/email/direccion/notas, activo, timestamps; trigger `set_updated_at` reutilizado; índice user+created). Nuevo hook `useMerchants.ts` (CRUD con React Query + `logAudit` de P4-19 → 3 acciones nuevas `merchant_*` en `auditLog.ts`). `Establishments.tsx` reescrito para usar el hook (ids ahora uuid, mutaciones async, estado de carga y botones deshabilitados al guardar). Se eliminó el tipo huérfano `src/types/establishment.ts`. Manual nuevo `establecimientos.md` + registrado en `manuals.ts`/`Help.tsx` (icono `Store`). `types.ts` parchado a mano con la tabla (regenerar tras aplicar). Verificado con `tsc --noEmit` y `eslint` (limpio). **Falta**: que el propietario aplique la migración y regenere types. Nota: esto es el CRUD de la entidad; asociar automáticamente `transactions.establecimiento` (texto libre) a un `merchant_id` queda como mejora futura si se quiere |
| P4-4 | TS estricto gradual (37 `any` legados, 59 errores lint preexistentes) | 📋 | |
| P4-5 | CORS de funciones restringido al dominio | ✅ | 2026-07-21: nuevo helper compartido `supabase/functions/_shared/cors.ts` (`corsHeadersFor(req, extraAllowHeaders?)`) que refleja el `Origin` SOLO si está en una allowlist, en vez del `Access-Control-Allow-Origin: *` que tenían las 9 funciones. Allowlist configurable por env `ALLOWED_ORIGINS` (coma-separado); default: `misfin.co`, `www.misfin.co` + `localhost:8080`/`localhost:5173` de desarrollo. Migradas las 8 funciones de navegador (auth-with-any-email, add-user-email, send-otp, verify-otp, send-reset-password, verify-reset-password, send-welcome-email, verify-user-email) — el `corsHeaders` pasó de constante de módulo a local por-request. `process-email-transaction` también migrada, conservando el header `x-webhook-secret`; su flujo webhook (server-to-server, sin `Origin`) no se ve afectado. `process-email-queue` no tiene CORS (solo cron). Añade `Vary: Origin`. **Falta**: que el propietario redepliegue las funciones (`supabase functions deploy`) y, si el dominio de producción final difiere (ej. dominio de Netlify durante la transición), configure el secret `ALLOWED_ORIGINS`. No se pudo `deno check` en el sandbox (Deno no instalado); verificado visualmente que ningún uso de `corsHeaders` quedó fuera del handler |
| P4-6 | Auditoría dedicada de `auth-with-any-email` | 📋 | Ver security-review |
| P4-7 | Multi-moneda en transferencias | 📋 | Hoy usa moneda de la cuenta origen |
| P4-8 | Multi-espacio (`space_id` hogar/empresa) | 🤔 | Base futura B2B |
| P4-9 | E2E (Playwright) de flujos críticos | 📋 | |
| P4-10 | Decidir futuro de simuladores (Créditos, Suscripciones) | 🤔 | |
| P4-11 | Validación responsive en dispositivos reales + Lighthouse | 📋 | Checklist en `docs/ui-review.md`; requiere app corriendo con BD migrada (P0) |
| P4-14 | Salida de Lovable (código) | ✅ | Sin `lovable-tagger`, metas OG propias, README propio, fallback IA migrado a Claude (`ANTHROPIC_API_KEY`). 2026-07-21: se eliminaron del repo `scripts/load-lovable-data.mjs` y `scripts/migrate-from-lovable.mjs` (scripts de un solo uso, ya ejecutados, migración verificada 1:1 en P0-1) — quedan en el historial de git si algún día hace falta consultarlos. Pendientes de borrar manualmente por el propietario (fuera de git, contienen datos reales): `.env.lovable.backup` y `scripts/lovable-load/` |
| P4-15 | Hosting en Netlify | 🔨 | `netlify.toml` + `_redirects` listos; falta conectar el repo en Netlify y configurar variables `VITE_*` (acción del propietario) |
| P4-16 | Cerrar cuenta/plan de Lovable | 🤔 | Antes verificar: acceso directo al proyecto Supabase `bymdreiwahvjejhjubrp` desde supabase.com (dueño real), y transferir dominio/DNS (`misfin.co`) a Netlify sin caída |
| P4-17 | Estrategia de ramas Git (cuando haya usuarios reales) | 📋 | Decisión 2026-07-18: por ahora todo directo en `main` (publica de una vez). Más adelante: `main` protegida + ramas `feat/*` + PR, y `dev`/`uat` ya existen como base |
| P4-18 | Migrar `react-day-picker` v8 → v9 y quitar `legacy-peer-deps` | 📋 | v8 declara peer date-fns v2/v3 vs. v4 del proyecto; hoy se resuelve con `.npmrc` (necesario para Netlify). v9 elimina el conflicto pero cambia la API del componente Calendar |
| P4-19 | Auditoría: visor de `audit_logs` + registrar operaciones clave del cliente | ✅ | 2026-07-21: agregada la pestaña "Actividad" en `ProfileDialog.tsx` (`useAuditLogs.ts`, lista de solo lectura, más reciente primero, hasta 100 registros) — reutiliza `ScrollArea`/`Badge` ya existentes en el proyecto, con formato relativo ("hace 3 horas") sin agregar `date-fns` como dependencia nueva (`src/utils/auditLog.ts`). **2026-07-21 (completado)**: se agregó el registro de las mutaciones financieras que faltaban. Nuevo helper `src/lib/audit.ts` (`logAudit`) — "best-effort": si el insert a `audit_logs` falla NO tumba la mutación principal (solo `console.warn`), y resuelve el `user_id` solo si el llamador no lo pasa. Conectado en los tres hooks: `useTransactions` (crear/editar/eliminar + `transfer_created`), `usePaymentMethods` (crear/editar/eliminar cuenta), `useBudgets` (crear/editar/eliminar presupuesto). 13 códigos de acción nuevos con etiquetas legibles en `ACTION_LABELS` (`auditLog.ts`), más test unitario (`src/__tests__/utils/auditLog.test.ts`). No hizo falta migración: la política RLS "Users can insert their own audit logs" ya existía (`20260718000400`). Verificado con `tsc --noEmit` y `eslint` (limpio); los tests no se pudieron correr en el sandbox (binario nativo de rolldown ausente para la arquitectura) — correr `npm run test:run` localmente. **Nota**: esto NO cubre todavía P7-6 (auditoría de accesos administrativos/exportación masiva de datos de contacto) — eso sigue pendiente y depende de que exista un panel de ese tipo primero |
| P4-20 | Perfil: foto/avatar y eliminación de cuenta | 📋 | Hoy el perfil solo edita nombre y correos; borrar cuenta (con exportación previa) es deseable para confianza y normativa de datos |
| P4-28 | Onboarding: selección de tema (color + claro/oscuro/sistema) en la primera visita | ✅ | 2026-07-21, pedido del propietario. Al investigar se encontró que el modo oscuro no tenía **ningún** mecanismo real (columna `modo_oscuro` sin ninguna UI ni lógica que la leyera — código muerto) y que el color de tema (`color_theme`) se aplicaba con dos implementaciones desconectadas (`useColorTheme` un `useState` sin contexto compartido, y `useIconTheme` sin usar en ningún lado) — se consolidó todo en `src/contexts/ThemeContext.tsx` (`AppThemeProvider`/`useAppTheme`), montado una sola vez en `Layout.tsx`. Nueva columna `user_config.tema_modo` ('claro'/'oscuro'/'sistema', migración `20260721140000_theme_mode_column.sql`; `modo_oscuro` queda marcada obsoleta, sin eliminar). Nuevo componente `ThemeOnboardingStep.tsx`: paso previo al tour de funciones en el dashboard (`Index.tsx`), con vista previa en vivo (cambia el tema real de la app detrás del overlay, no una captura). `SettingsDialog.tsx` también gana el selector de apariencia que no existía. **Nota:** se editó `src/integrations/supabase/types.ts` a mano para agregar `tema_modo` (no se pudo correr `supabase gen types` en el sandbox) — regenerar cuando el propietario aplique la migración, para no dejar el archivo generado desincronizado de la BD real |
| P4-21 | Ayuda en menú de usuario con FAQ y WhatsApp | ✅ | 2026-07-18: /ayuda abre con "Primeros pasos" por defecto; FAQ integrada; línea de atención wa.me/573125655293; sidebar sin sección Ayuda |
| P4-12 | Tipografía con números tabulares (Inter) + montos adaptativos en móvil | ✅ | 2026-07-21: R1 ya estaba hecho de una sesión previa (Inter+Poppins cargados en `index.html`, activados en `tailwind.config.ts` `fontFamily`, y `font-variant-numeric: tabular-nums` global en `body` de `index.css` — todas las cifras alinean dígitos). R2 (montos adaptativos): `BalanceCard` y las tarjetas de resumen de Transacciones ya usaban `text-xl sm:text-2xl ... break-words`; se agregó el mismo tratamiento a las tarjetas de resumen de `Reports.tsx` (Presupuesto/Gastado/Disponible) y `break-words` a las cifras de Deuda/Haber de las cuentas en el dashboard (`Index.tsx`). Verificado con `tsc`. Nota: el "formato miles por defecto en móvil" que sugería R2 no se aplicó a propósito — pisaría la preferencia `formato_cifras` del usuario; `break-words` ya evita el desborde |
| P4-13 | Accesibilidad: aria-labels legados, foco visible, reduced-motion | ✅ | 2026-07-21 (R5 de `docs/ui-review.md`): (1) **reduced-motion** — `<MotionConfig reducedMotion="user">` global en `App.tsx` hace que TODAS las animaciones de framer-motion respeten `prefers-reduced-motion` sin tocar cada componente; complementado con un `@media (prefers-reduced-motion: reduce)` en `index.css` que reduce animaciones/transiciones CSS y de Tailwind. (2) **aria-labels** — agregados a los botones de solo icono de pantallas legadas: `TransactionList` (editar/recurrente/eliminar/más opciones) y `Categories` (editar/eliminar categoría). (3) **foco visible** — verificado que el `Button` de shadcn ya trae `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2`, y que no hay `<div>`/`<Card>` clicables sueltos actuando como botón (todas las acciones son `Button` reales, ya enfocables por teclado). Verificado con `tsc` y `eslint` (sin errores nuevos; los `any` que reporta son preexistentes, P4-4). Falta solo la validación en dispositivos reales + Lighthouse, que es P4-11 |
| P4-22 | Internacionalización completa (i18n) de la app | 🤔 | Tráfico real desde EE.UU. y Francia (visto en GA4). 2026-07-19: se hizo una versión acotada (solo Landing, ES/EN/FR, selector manual, sin framework) — ver P4-23. Pendiente decidir: framework (`react-i18next`) para cubrir dashboard + 12 manuales de ayuda, traducción humana vs. IA, `hreflang`/SEO multi-idioma, detección automática por navegador/geo |
| P4-23 | Selector de idioma en Landing (ES/EN/FR) | ✅ | 2026-07-19: diccionario propio en `src/lib/i18n/landing.ts`, selector en el header, persistencia en localStorage + parámetro `?lang=`. Cubre toda la Landing pública; el resto de la app (dashboard, manuales) sigue solo en español — ver P4-22 |
| P4-24 | CI: quality gate de cobertura de pruebas en cada release | ✅ | 2026-07-20: `.github/workflows/ci.yml` — lint (informativo, ver P4-4), `tsc --noEmit`, `npm run test:coverage` y `npm run build`. El piso de cobertura en `vite.config.ts` se bajó de la meta aspiracional (60/60/50%) al real medido (~1.25/0.65/1.38%) para que el gate sea "no regresión" desde hoy, no un bloqueo total; subirlo gradualmente a medida que P4-2 agregue tests. Falta activar "Require status checks to pass" en Settings → Branches del repo (acción manual del propietario en GitHub, no vía código) |
| P4-25 | DevSecOps: escaneo de vulnerabilidades en el pipeline | ✅ | 2026-07-20: `.github/workflows/security.yml` (Gitleaks + Semgrep, gratis, corre en cada push/PR a `main` y semanalmente) + `.github/dependabot.yml` (alertas de CVEs npm y GitHub Actions, gratis incluso en repo privado). `CodeQL` no se incluyó: gratis solo en repos públicos, este repo es privado. `Trivy` quedó fuera por ahora (más orientado a contenedores/IaC, que este proyecto no usa todavía) |
| P4-26 | Code-splitting del bundle de producción (hoy 2.49 MB / 611 KB gzip en un solo archivo) | ✅ | 2026-07-21: `App.tsx` — todas las páginas excepto `Landing` (la puerta de entrada pública) ahora se cargan con `React.lazy` + `Suspense` (fallback: `Loader` ya existente), envueltas dentro del `<Routes>`. `vite.config.ts` — `manualChunks` separa `react`/`react-router-dom`, `recharts` y `@tanstack/react-query` en chunks propios, cacheables por el navegador aparte del código propio. Verificado con `tsc --noEmit`; falta que el propietario corra `npm run build` localmente para confirmar el tamaño final de los chunks (no se corrió build en el sandbox, ver política de verificación) |
| P4-27 | `useTransactions`: mutaciones deben recibir `category_id`/`subcategory_id` directo, no resolver por nombre | ✅ | 2026-07-21: `TransactionDialog.tsx` ahora resuelve `category_id`/`subcategory_id` del `<Select>` en el momento del submit y los manda junto con `categoria`/`subcategoria`. `createTransaction`/`updateTransaction` en `useTransactions.ts` los usan directo si vienen, y solo resuelven por nombre como *fallback* (para no romper otros llamadores). De paso corrige un bug latente: al editar una transacción y quitar la subcategoría, antes se quedaba con el `subcategory_id` viejo si el nombre no matcheaba; ahora se limpia correctamente. Verificado con `tsc --noEmit` |
| P4-29 | Unificar diseño Landing (Personas) y LandingEmpresa — componentes compartidos | ✅ | 2026-07-21, pedido del propietario. Antes las dos landings públicas no compartían ningún componente (header y footer duplicados e independientes, ya divergidos: Personas fixed+scroll-shadow+i18n, Empresas sticky plano sin i18n). **Fase 1**: `src/components/landing/PublicHeader.tsx` y `PublicFooter.tsx`, usados por ambas páginas — logo, badge de audiencia, menú móvil y scroll-shadow viven en un solo lugar ahora. De paso se corrigió un hueco real de cumplimiento: `LandingEmpresa` no tenía links a Términos/Tratamiento de Datos/Cookies, ahora los tiene (vía `PublicFooter`). **Fase 2 (2026-07-21)**: Empresas adopta el mismo lenguaje visual de Personas — se extrajo `ImageCarouselBackdrop.tsx` (fondo animado con carrusel + overlay "hero" claro u overlay "duotono" esmeralda + círculos difuminados) y `heroBackgrounds.ts` (las 3 fotos, ahora compartidas por ambas landings en vez de que Empresas tuviera su propia imagen estática). Aplicado a: hero de Empresas (antes gradiente plano → ahora carrusel + tarjeta de vidrio esmerilado igual a Personas), sección "Técnicas de Flujo de Efectivo" (antes `Card` planas sobre `bg-muted/30` → ahora tarjetas de vidrio sobre fondo duotono, igual que Testimonios de Personas), tarjeta de precios (antes `Card` genérica → ahora con borde-2 + badge destacado igual al plan Premium de Personas, sobre el mismo fondo duotono que la sección de precios de Personas). De paso, Personas también quedó más simple: su hero y su sección de precios ahora usan el mismo componente compartido en vez de tener la lógica de carrusel duplicada dos veces dentro del propio archivo. Verificado con `tsc --noEmit` y `eslint` (sin errores nuevos) |

## P5 — Monetización

| # | Ítem | Estado | Notas |
|---|---|---|---|
| P5-1 | Suscripción de pago con Stripe — plan completo | 🔨 | **Decisión final 2026-07-20: Stripe**, con Elements embebido (no Checkout hospedado). Plan de implementación detallado en `docs/stripe-integration-plan.md` (modelo de datos, edge functions, fases). Comparativo con Paddle/Lemon Squeezy/Polar/PayU/Mercado Pago documentado en el chat. Precio: USD 2/mes, anual con 15% dto. (≈ USD 20.40/año). Bloqueado en la fase 1: requiere que el propietario cree la cuenta de Stripe (modo test) y comparta las API keys — ver "Siguiente paso concreto" al final del plan |
| P5-1a | Migración: tablas `subscriptions` + `stripe_webhook_events` + RLS | 📋 | Depende de P5-1 fase 1 (cuenta de Stripe). Diseño ya definido en `docs/stripe-integration-plan.md` |
| P5-1b | Edge functions: `create-subscription`, `stripe-webhook`, `stripe-customer-portal` | 📋 | Depende de P5-1a |
| P5-1c | Frontend: `useSubscription`, `SubscribeDialog` (Stripe Elements), conectar `PricingSection` a Price IDs reales, botón de portal en Perfil | 📋 | Depende de P5-1b |
| P5-6 | Aplicar límites del plan gratuito en el código (hoy solo es copy de marketing) | 🤔 | 50 transacciones/mes, 1 cuenta, 5 categorías — decidir si se valida en cada hook de mutación o con una función/trigger de Postgres (más robusto). Ver detalle en `docs/stripe-integration-plan.md` |
| P5-2 | Selector mensual/anual visible en la sección de precios de la Landing | ✅ | Ya existía el toggle Mensual/Anual en `PricingSection` (Landing.tsx); 2026-07-20 se actualizó al nuevo precio (USD 2/mes) y ahora también respeta el idioma seleccionado (ES/EN/FR, ver P4-23). Sigue pendiente de P5-1 para conectar los IDs de producto/precio reales de la pasarela elegida |
| P5-3 | Modelo de datos: suscripciones, estado de licencia por usuario, período de gracia | 🤔 | Tabla `subscriptions` (o similar) sincronizada por webhook de la pasarela; definir qué pasa con el acceso si el pago falla (gracia, downgrade a plan gratis, bloqueo) |

## P6 — Observabilidad

| # | Ítem | Estado | Notas |
|---|---|---|---|
| P6-1 | Agente de observabilidad para APIs (edge functions + frontend) | 🤔 | 2026-07-20: recomendación inicial **Sentry** (tiene tier gratis generoso: ~5K errores/mes + performance tracing; SDK oficial para Deno/edge functions y para React) — captura errores no manejados, releases, y latencia por transacción/endpoint. Complementar con lo que ya viene gratis en Supabase: **Logs** de cada edge function (Dashboard → Logs) y **Database → Query Performance** para SQL lento. Para uptime del sitio: **UptimeRobot** (gratis, chequeos cada 5 min + alertas). Evaluar también **Highlight.io** (session replay + APM, tier gratis) como alternativa todo-en-uno a Sentry si se quiere ver qué hacía el usuario antes del error, no solo el stack trace |

## P7 — Onboarding, verificación y cumplimiento legal

| # | Ítem | Estado | Notas |
|---|---|---|---|
| P7-1 | Verificación de celular vía OTP — **decidido: WhatsApp Business API** | 📋 | 2026-07-21: decisión tomada — WhatsApp en vez de SMS, ≈66x más barato en Colombia (≈USD 0.0008/msj vs ≈USD 0.0525/SMS Twilio; ≈USD 8-10 vs ≈USD 525-650 para 10.000 usuarios) y más usado por los usuarios objetivo. Diseño completo en `docs/phone-verification-design.md` (mismo patrón que el OTP de correo ya existente: edge functions `send-phone-otp`/`verify-phone-otp`, tabla `phone_verification_tokens`, columna `profiles.celular_verificado`). No implementado: falta que el propietario cree la cuenta de WhatsApp Business API (Twilio o Meta directo) y registre la plantilla de autenticación — bloqueado en esa acción, igual que Stripe en P5-1 |
| P7-1b | Badges de verificación en Correo/Celular del Perfil | ✅ | 2026-07-21: `ProfileDialog.tsx` — Correo Electrónico Principal muestra "Verificado"/"Pendiente de confirmación" según `user.email_confirmed_at` (dato nativo de Supabase Auth, ya existía sin usarse) con botón "Reenviar confirmación" (`supabase.auth.resend({type:'signup'})`, API nativa, no se había usado antes). Celular muestra "Pendiente de verificación" siempre honesto (no hay OTP de SMS aún, ver P7-1) sin botón de reenviar. Verificado con `tsc --noEmit` |
| P7-2 | Selector de moneda en el signup/onboarding, con valor por defecto según el país elegido | ✅ | 2026-07-21: `CurrencySelect` agregado al formulario de signup en `Landing.tsx`, se autocompleta al elegir país vía `src/utils/countryCurrency.ts` (CO→COP, zona euro→EUR, resto→USD; el proyecto solo soporta esas 3 monedas hoy) y el usuario puede sobreescribirla manualmente. Se persiste en `user_config.moneda` vía el trigger `handle_new_user` (migración `20260721120000_default_currency_on_signup.sql`), antes siempre quedaba en el default de columna `'COP'` sin importar el país. También agregado a `ProfileDialog.tsx` ("Mi Perfil") para usuarios ya registrados — el propietario lo señaló tras ver el modal sin ese campo. Verificado con `tsc --noEmit`; falta que el propietario aplique la migración (`supabase db push`) y confirme en producción |
| P7-3 | EULA formal con aceptación diferenciada (plan gratuito con publicidad vs. plan pago) + copia enviada por correo | 🔨 | 2026-07-21: borrador redactado en `docs/eula-draft.md`, marcado explícitamente "no es asesoría legal, revisar con abogado" (autorizado así por el propietario). Cubre licencia de uso, diferenciación plan gratuito-con-publicidad vs. plan pago, exoneración de responsabilidad financiera (importante en una app de finanzas), ingesta de correos, terminación y ley aplicable (Colombia, con nota sobre usuarios en otros países). Trae una sección de notas para el revisor con los campos pendientes de completar (razón social, límites reales) y las dependencias de producto (P5-6, P7-8). **Falta**: revisión por abogado, y construir el flujo de aceptación + envío de copia por correo (reutilizando Resend) — eso sigue sin empezar |
| P7-4 | Modal de datos obligatorios pendientes para usuarios ya registrados | 📋 | Reutilizar el mecanismo de `profiles.onboarding_completed` (hoy solo dispara el tour de funcionalidades en `OnboardingTour.tsx`/`Index.tsx`) para detectar campos obligatorios faltantes (país, moneda, celular verificado, aceptación de EULA vigente) y mostrar un modal de completar registro; los mismos campos editables luego desde `ProfileDialog.tsx`. Depende de P7-1/P7-2/P7-3 |
| P7-5 | Revisar si Términos/Tratamiento de Datos necesitan versión por país | 🤔 | Hoy `/terminos` y `/tratamiento-datos` son una sola versión (pensada para Colombia/habeas data Ley 1581). Hay tráfico real de EE.UU. y Francia (visto en GA4, ver P4-22) — evaluar si alcanza con la política única + banner de cookies (Consent Mode v2 ya instalado, P0) o si hace falta diferenciar GDPR (UE) / CCPA (EE.UU.) explícitamente |
| P7-6 | Auditar accesos administrativos a datos de contacto (no solo mutaciones financieras) | 🔨 | 2026-07-21: se construyó la pieza de acceso — nueva página `/admin/auditoria` (`AdminAudit.tsx`), visible solo para `super_admin` (`andres.montoya@piensait.com` ya tiene el rol, ver migración `20251202020037`), con acceso rápido desde la pestaña "Actividad" de Mi Perfil. Política RLS nueva en `audit_logs` (migración `20260721130000_audit_logs_super_admin_policy.sql`, reutiliza `has_role()` ya existente, mismo patrón que `email_staging`) — es política *adicional* (Postgres combina con OR), así que cada usuario sigue viendo solo lo suyo por la política original. **Falta lo que pediste originalmente**: hoy solo hay 3 tipos de evento auditados (todos de sugerencias de correo) — no existe todavía el registro de "quién consultó/exportó datos de contacto de forma masiva" en sí, eso sigue pendiente y depende de que exista un panel de ese tipo primero (no hay ninguno hoy, ver `docs/user-data-governance.md` §3) |
| P7-7 | Señales de fraude en el registro (hoy no hay ninguna) | 🤔 | 2026-07-21, pedido del propietario. Hoy solo existe rate limit de *login* por IP (`auth-with-any-email`); el signup no tiene ningún control. Propuesta en `docs/user-data-governance.md` §4-5: volumen de registros por IP, dominios de correo desechables, reutilización de un mismo celular verificado en corto tiempo — todo como señal para *revisar*, no bloquear automáticamente (el costo de un falso positivo en una app financiera es alto) |
| P7-8 | Consentimiento de marketing persistido por usuario (correo/WhatsApp) | 🤔 | 2026-07-21, pedido del propietario: la base de contactos es insumo valioso de marketing, pero hoy no existe ningún campo de consentimiento en BD (solo el consentimiento de cookies del sitio, que es de navegador, no de cuenta). Propuesta de columnas en `docs/user-data-governance.md` §2. **Depende de P7-3** (EULA free-con-publicidad vs. pago) para no migrar dos veces — si el plan gratuito incluye publicidad como parte de sus condiciones, el consentimiento no es un checkbox opcional aparte sino parte de aceptar el plan |
