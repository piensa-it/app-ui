# Dependencias
node_modules/

# Build
dist/
storybook-static/
*.tsbuildinfo
.tmp-react18-consumer-*/

# Tests
coverage/
playwright-report/
test-results/
.playwright-mcp/

# Capturas de referencia de macOS (#141): son específicas de cada Mac, no de
# la plataforma, y nadie las compara en CI (browser-gate corre en Linux). Las
# genera sola `npm run test:browser` en la primera corrida de cada máquina.
tests/browser/__screenshots__/**/*-darwin.png

# Entorno
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json

# Claude Code — preferencias personales, no se comparten con el equipo
# (las instrucciones compartidas viven en .claude/CLAUDE.md, sí versionado)
CLAUDE.local.md
.claude/settings.local.json
