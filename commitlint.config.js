/**
 * Conventional Commits (feat/fix/refactor(scope): mensaje) — el mismo
 * formato que ya se viene usando en el historial de este repo. Sienta la
 * base para generar el CHANGELOG automáticamente más adelante.
 */
export default {
  extends: ["@commitlint/config-conventional"],
};
